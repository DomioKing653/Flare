# Flare

## Building code

### 3 ways to build code

- ``build <src> <output>`` builds file to ``<output>`` from ``<src>`` as source
- ``run <src>`` runs compiled file from ``<src>``
- ``exec <src> <output>`` runs build + run